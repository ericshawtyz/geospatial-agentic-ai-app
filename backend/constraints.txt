# Pin agent-framework packages to known-compatible versions.
# These packages must stay in sync — mismatched versions cause ImportErrors.
# Only the packages we actually use — NOT the agent-framework meta-package
# (which pulls in agent-framework-core[all] with 20+ optional sub-packages).
agent-framework-core==1.0.0rc6
agent-framework-openai==1.0.0rc6
agent-framework-azure-ai==1.0.0rc6
agent-framework-foundry==1.0.0rc6
